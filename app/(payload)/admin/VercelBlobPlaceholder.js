'use client'

/**
 * Placeholder for Vercel Blob client upload handler.
 * We use clientUploads: false so the real handler is never used; this satisfies the importMap lookup.
 */
export function VercelBlobClientUploadHandlerPlaceholder() {
  return null
}
